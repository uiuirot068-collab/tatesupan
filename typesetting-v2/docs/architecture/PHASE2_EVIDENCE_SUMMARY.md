# Phase 2 — Evidence Summary

- Status: Phase 2 closeout artifact, pending Human Gate (see `PHASE2_ARCHITECTURE_NARROWING.md`)
- Full detail for every loop lives in `typesetting-v2/research/PHASE2_LOOP_LOG.md`; this document is a navigable index, not a replacement for it.
- HOLD loops are included deliberately — the history of what didn't work the first time is part of the evidence, not something to hide.

| Loop | Question | Result | Human decision | What it changed |
|---|---|---|---|---|
| P2-L01 | Which candidate architecture (C1/C2/C3) can produce visibly better, controllable vertical Japanese composition? | Real evidence gathered for all three on 文庫; C2 (HarfBuzz) shaping measured directly — `vpal` nudges offset only, never advance, so dedicated shaping alone doesn't close the punctuation-gap; first empirical confirmation that native Chromium print-to-PDF produces genuinely selectable vertical text | C1 & C3 PASS for 文庫 (near-tie, C3 marginally preferred); C2 DEFERRED (not rejected — comparison presentation was clipped) | Established the 3-candidate comparison harness and the shaping-evidence method reused through the rest of Phase 2 |
| P2-L01B / P2-L01C | Can the P2-L01 comparison be made fair/legible for Human QA (no diagnostic clutter, C2 not clipped)? | B: partial fix, ran out of time (HOLD). C: wiring completed, re-verification blocked by tooling guard | — | Cleaned up `comparison.html` presentation; C2 remained deferred throughout |
| P2-L02 | Do C1 and C3 hold up across all 8 mandatory presets, not just 文庫? | 8/8 presets compared with real preset values; a real A5-2段 stacking-model bug caught and fixed (documented, not hidden) | C1 preferred on 5/8 (文庫, A5 1段, A5 2段, B6, A6); C3 preferred on 2/8 (B5, Web閲覧用); 新書 judged in-between; not a uniform winner either way | Ruled out "one architecture always wins"; set up the preset-dependent puzzle P2-L03 diagnosed |
| P2-L03 | Why is the Human result preset-dependent — real architecture limitation, or PoC formula bug? | Read real `src/lib/pageLayout.ts`/`PageCard.tsx` formulas (read-only): capacity is always 1em/char; a separate `gridMode:"justified"` visual-stretch mechanism (already shipped, not invented) explains B5/新書's "too loose" as a REAL, already-shipped preset property, correctly reproduced — not a PoC bug. Web閲覧用's "too cramped" WAS a genuine PoC unit-conversion bug | — (diagnostic loop, no new Human pass) | Traced B5/新書 to a real product-configuration property (in tension with Master §5.2) and Web to a PoC bug specifically |
| P2-L04 / P2-L04B | Can deterministic explicit layout use natural (1em) pitch instead of legacy page-fill stretching, fixing B5/Web without breaking 文庫/A5? | "C1-NATURAL" variant built (Production's own existing `gridMode:"solid"`, generalized) plus a corrected Web unit model; a real C3-fixture-alignment CSS bug found and fixed (B) | C1-NATURAL preferred on 3/5 tested presets (A5 1段, 新書, Web閲覧用); C1-JUSTIFIED preferred on 文庫; C3 preferred on B5; both prior defects (B5-loose, Web-cramped) judged IMPROVED under C1-NATURAL | Established C1-NATURAL as the working hypothesis carried through the rest of Phase 2 |
| P2-L05 | Can C1-NATURAL structurally support kinsoku/hanging/ruby/TCY/dash/ellipsis via a deterministic, source-mapped decision layer — not just natural pitch? | Built a real tokenize→units→rule-decision→line engine (`scripts/engine.js`), 11 fixtures, all deterministic (verified twice each) | HOLD on presentation only — diagnostic-box view was not judgeable; approved *continuing the structure* | Proved the architecture question (separable rule-decision layer) independent of visual polish |
| P2-L06 / P2-L06B | Can those decisions render as a true, publication-like vertical page? | Two real rendering bugs found+fixed (flex/orthogonal-writing-mode column overlap; `display:block` stacking on the wrong axis); ruby group-association fixed (one `<rt>` per group, not per character); TCY visual combine found broken (root cause not resolved within L06/L06B's timebox) | Kinsoku/Hanging/Ruby(-grouping) PASS; TCY/Dash/Ellipsis judged not correct but **explicitly deferred to Phase 3** by Product Owner, not blocking | First true vertical-page renderer; TCY/dash/ellipsis formally moved off the Architecture-Narrowing critical path |
| P2-L07 | Does ruby, once grouped correctly, actually leave body-text position unchanged? | Root cause of "body shifts" found (native `<ruby>`'s un-sized block extent vs. explicitly-sized `.cell`s); fixed by rendering base chars identically regardless of ruby presence, ruby as a separate `position:absolute` overlay; automated code-level invariant added and PASSED | Base-position invariant confirmed at the code level; annotation itself still wrapped (new defect found) | Separated "does ruby move the body" (solved) from "does the ruby annotation itself render correctly" (not yet) |
| P2-L07B | Fix the ruby annotation wrapping into a second column | Root cause: annotation box height was sized from the base's cell count, not the reading's own extent; fixed generically (narrow fixed width, `height:auto`) | 東京 natural; 其 (long ruby) no longer wraps, but un-centered placement judged NOT ACCEPTABLE — Human explicitly rejected a `baseLength==1` special case | Ruby no longer wraps; centering became the next question |
| P2-L07C | Center overlong ruby generically (not by base-character-count) | `rubyExtent > baseExtent` rule implemented once, verified against 4 fixtures incl. a purpose-built multi-char-base case and a genuine non-overlong control; a stale assumption about 東京's reading length (4 vs. actual 5 chars) caught and corrected | Centering itself judged natural, but Human reported the BODY visibly moved — contradicting the code-level invariant | Surfaced the need for an ACTUAL rendered-DOM measurement, not just a logical-coordinate claim |
| P2-L07D | Add line-start/line-end boundary clamping + real rendered-DOM body measurement | `decidePlacement` (CENTER/START_CLAMP/END_CLAMP/OVERFLOW_OPEN) implemented; a live `getBoundingClientRect()` body-invariant panel added; **an anomaly was found and honestly reported, not resolved**: the evidence table showed all three boundary fixtures as CENTER when 2 should have clamped | HOLD — explicitly not claimed as PASS | Left ruby's boundary behavior an open, disclosed question rather than a false PASS |
| P2-L07E | Root-cause the P2-L07D anomaly | Wrote a real, importable unit-test script (`scripts/verify-ruby-boundary-policy.js`) against the real `decidePlacement`/`rubyGeometry` functions; found the anomaly was a **display-only bug** in the evidence table (a stale binary check never updated to read the real `placement` field) — the renderer itself was correct throughout; fixed and re-verified twice independently | — (diagnostic loop; final Human ruby pass recorded directly under P2-L08's freeze) | Resolved the last open architectural question about ruby: body-position invariant, no-wrap, and boundary-aware centering are all now independently verified correct |

## Final Human ruby gate (recorded under P2-L08)

東京: ruby association natural (YES), body axis unchanged (YES — rendered delta 0.00px main-axis / 0.00px cross-axis for both 東 and 京, per the live measurement panel), placement natural (YES). CENTER: Human PASS. START_CLAMP: Human PASS. END_CLAMP: Human PASS. 其: Human PASS. This closes the Ruby Architecture-Narrowing gate.

## Deferred by explicit Human/Product Owner decision (not Architecture-Narrowing blockers)

TCY visual renderer, dash final alignment, ellipsis final alignment — all judged not yet correct in P2-L06, explicitly deferred to Phase 3 Renderer work. C2 (dedicated shaping) — deferred, not rejected, reopenable.
