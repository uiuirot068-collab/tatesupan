# Stage D — Preview Development Adapter Implementation

- Status: **IMPLEMENTED. Human Visual QA: PENDING.** A non-Production, React absolute-position painter renders 11 fixtures (including both mandatory Human QA fixtures) from real, composed `CanonicalDocument`s into a static, self-contained HTML file a Human can open directly in any browser. No `src/`, Production, dependency, or config change.
- Preflight: branch `design/tatespun-typesetting-v2`, HEAD `597b2affb2a98d376fd28933ba88e4e0e2a57227` (matches expected checkpoint — the paragraph semantics implementation), worktree clean before start.

---

## 1. Verdict

**Stage D implementation: PASS.** The approved architecture (`qa/research/PREVIEW_DEVELOPMENT_ADAPTER_PLAN.md` — React DOM absolute-position, disposable-implementation/evolutionary-contract, `CanonicalDocument` + read-only `LogicalUnit[]` lookup, paint-only) is implemented under `typesetting-v2/tools/preview-dev-adapter/`. It paints already-decided Core coordinates only — verified by a direct test asserting the `CanonicalDocument` a fixture composes to is byte-identical before and after building a view model from it. A real, openable artifact exists at `typesetting-v2/qa/visual/stage-d/index.html` (~86KB, generated by `npx vitest run`, no bundler, no server, no new dependency). **Human Visual QA has not been performed — that is this document's own next step, not something this Loop can complete on its own.**

---

## 2. Architecture

- `geometry.ts` — the single, centralized, one-way `GeometryTick -> px` conversion (`tickToPx`). Deterministic; scale is a plain display-zoom multiplier; never reads a browser measurement; never feeds back into Core.
- `viewModel.ts` — `buildPreviewViewModel()`: the only place `CanonicalDocument` + the caller's own `LogicalUnit[]` + a `PreviewRenderContext` become paintable coordinates. Never re-tokenizes, re-runs kinsoku, chooses a break, or derives capacity — confirmed by a direct immutability test (§12).
- `PreviewApp.tsx` — the React painter. Every positioned element uses `position:absolute` + explicit px read from the view model; no browser text flow, no CSS `writing-mode` as layout authority, no flex/grid auto-placement for canonical content (the outer page-to-page layout uses flexbox only for wrapping multiple *pages* side by side, never for placing content *within* a page).
- `fixtures.ts` — reuses Stage C's own `buildFixtureUnits` (`typesetting-v2/tools/compare/fixtureBuilder.ts`) rather than re-authoring the same hand-construction logic a second time.
- `generateArtifact.test.ts` — composes every fixture through the real `composeCanonicalDocument`, builds view models, renders via `react-dom/server`'s `renderToStaticMarkup` (already an installed dependency — no new one added), and writes the static HTML artifact to disk as a test side effect.
- A dedicated `vitest.config.ts` (new file, mirrors Stage C's own pattern) — the root `vitest.config.ts` stays on this Loop's do-not-modify list and its `test.include` remains scoped to `core/**`.

**Disposable/evolutionary lifecycle, restated:** this is not the final Preview Renderer (P3-O09) and does not decide that question. What is expected to survive into a future Renderer, regardless of technology, is the input-contract shape and the `tickToPx` discipline — not this specific React/CSS paint code.

---

## 3. Canonical Input Boundary

Exactly the contract the Preview Adapter plan recommends: `buildPreviewViewModel(id, label, document: CanonicalDocument, units: LogicalUnit[], source: string, ctx: PreviewRenderContext)`. `units` is the identical array the caller already passed into `composeCanonicalDocument` — used only for a read-only span-containment lookup (`findOwningUnit`) to recover a placed atom's kind and text, exactly mirroring Stage C's own `v2Adapter.ts` convention (including the same fix for the "one unit split into several placed atoms" duplication bug that convention exists to avoid — text is sliced from the placed atom's own span, never the owning unit's full span). Nothing in `viewModel.ts` or `PreviewApp.tsx` imports from `core/compose/`, `core/breaks/`, or any other decision-making module — only the public `core/index.ts` entry point and type-only imports.

---

## 4. GeometryTick → Visual Conversion

`tickToPx(tick, scaleMultiplier) = tick * 0.001mm/tick * (96/25.4)px/mm * scaleMultiplier`. Tested directly (`viewModel.test.ts`): identical inputs produce identical output; changing `scaleMultiplier` scales the output proportionally with no effect on any `GeometryTick` value; zero ticks always maps to zero px. No code path anywhere in this Stage feeds a computed px value back into a `GeometryTick`, a `PageCompositionSettings` field, or any Core function argument.

---

## 5. Rendered Feature Matrix

| Feature | Status |
|---|---|
| Plain text body | PASS |
| Multiple lines | PASS |
| Multiple columns | PASS (`two-column-flow` fixture) |
| Multiple pages | PASS, bounded (`multi-page` fixture, §11) |
| Manual page break | PASS — visibly marked, and verified to carry no phantom extra line (Root Cause C not ported) |
| Paragraph breaks | PASS (`paragraph-blank-line` fixture) |
| Automatic first-line indent | PASS — painted as a hatched leading-space marker sized from `CanonicalLine.indentTick`, never CSS `text-indent` |
| Blank paragraph | PASS — a genuine empty line renders (no units, no indent marker unless it happens to also open a new paragraph) |
| Natural Pitch residual | PASS — painted as a labeled green marker at each column's own trailing edge, sized from `residualSpaceTick`, never stretched into content |
| Source/debug metadata | PASS (§8) |
| Image placeholder | PASS — hatched rectangle sized from the derived extent, zero text |
| HOLD state | PASS (§10) |

---

## 6. Paragraph Semantics

The `paragraph-blank-line` fixture exercises all four Human Product Decision A/B behaviors from `PARAGRAPH_SEMANTICS_PRE_STAGE_D.md` visually: first paragraph indented, a `PARAGRAPH_BREAK`-terminated line followed by a freshly-indented second paragraph, a genuine blank line for the double break, and a third indented paragraph after the blank line. The indent itself is painted as `CanonicalLine.indentTick` converted to px — the Core's own logical decision, never a CSS `text-indent` invention (verified: `viewModel.test.ts`'s "surfaces the paragraph-break indent as line-level metadata" test confirms no extra `ViewPlacedUnit` is fabricated for it). The `manual-page-break` fixture confirms no phantom line accompanies v2's `ManualBreakUnit` (verified by both a unit test and by construction — v2 has no code path that would create one).

---

## 7. Special Units / Provisional Features

Every RUBY, TCY, SEMANTIC_RUN, and IMAGE unit is painted with a small "prov" badge and a distinct background tint, so a reviewer never mistakes v0 painting for approved typography:

- **Ruby** (`atomic-ruby` fixture): base text paints at its already-decided body position (Core-final); no reading annotation is painted at all in this v0 (the simpler of the two options `PREVIEW_DEVELOPMENT_ADAPTER_PLAN.md` §9 offered — a plain block with a "not yet separated" disclosure — was chosen over directly calling Core's own untested-in-this-context `placeRuby()`, given this Loop's own timebox; recorded as a deliberate, disclosed choice, not an oversight). Ruby placement wiring remains OPEN, unaffected by this choice.
- **TCY** (`explicit-tcy` fixture): the logical group paints as one tinted box at its correct position and width; no rotation/shaping is attempted (P3-O03 stays OPEN).
- **Dash/ellipsis** (`dash-ellipsis` fixture): plain glyphs at canonical positions, no optical alignment (P3-O04/O05 stay OPEN).
- **Colophon:** not included in this fixture set — Stage C already deliberately scoped colophon comparison out (`STAGE_C_LOGICAL_COMPARISON.md` §2), and this Loop does not reopen that scoping decision.
- **Folio/header:** not shown — `CanonicalPage.folio` is still never populated by `compose/page.ts` (unchanged since the Preview Adapter plan's own audit); nothing to paint.

---

## 8. Debug Overlays

Implemented as pure CSS (a hidden checkbox + `:checked` sibling selector) — **no client-side JavaScript exists in the artifact at all**, so it opens and behaves identically with no build step. Toggling reveals, per unit: page/column/line index (via each container's own always-visible index label plus the toggled per-unit badge), source span `[start,end)`, unit kind, and (for lines) the indent extent in px. Break-*reason* attribution (which specific kinsoku/paragraph rule fired at a given cut) is **not** included in this v0 — `TraceEvent`s exist per rule application but are not yet indexed back to individual placed units; recorded here as a disclosed second-pass omission, not a silent gap.

---

## 9. Known Stage C Residual

The `long-prose` fixture (the approved long non-repeating Human QA prose, re-authored with explicit `PARAGRAPH_BREAK` units) is included and paints normally. Its still-undiagnosed residual (`PARAGRAPH_SEMANTICS_PRE_STAGE_D.md` §9 — legacy's own observed line lengths on the 2nd/3rd paragraph don't match this Loop's re-derivation of legacy's indent behavior) is a **Stage-C-vs-legacy** comparison finding, not something a Stage D painter of v2's own output alone can visually surface or annotate — v2's own composed result for this fixture is internally consistent and paints correctly. This residual remains **OPEN INVESTIGATION, NON-BLOCKING for the first visual gate**, exactly as instructed: nothing in this fixture's v2-only rendering looked severe or unexpected on inspection during implementation.

---

## 10. HOLD / Diagnostics

The `hold-example` fixture (a single TCY unit whose declared 5-cell logical cost exceeds a deliberately tiny 2-cell line extent) triggers `SINGLE_ATOM_EXCEEDS_LINE_EXTENT`. Verified by direct test that `PreviewApp` renders this as a red HOLD banner with the error text, and — critically — **never** renders a `page-row` for it (the component's own hold-branch structurally excludes the normal page-painting branch, not just visually de-emphasizes it).

---

## 11. Performance / Page Window

`generateArtifact.test.ts` bounds every fixture to `MAX_PAGES = 6` (the Preview Adapter plan's own recommended default). The `multi-page` fixture's underlying `CanonicalDocument` may contain more true pages than that; `PreviewViewModel.totalPageCount` vs `renderedPageCount` are tracked separately and both asserted by a dedicated test, so bounding the paint never hides the fact that more pages exist. **All-pages-mounting was deliberately not copied from legacy's Preview** (Editor Inventory's own flagged risk) — confirmed by the bounding logic itself, not merely by absence of a counterexample.

---

## 12. Test Results

- `npx vitest run --config typesetting-v2/tools/preview-dev-adapter/vitest.config.ts`: **17/17 passing** (14 `viewModel.test.ts` + 3 `generateArtifact.test.ts`), including the CanonicalDocument-immutability test (a JSON snapshot taken before `buildPreviewViewModel` is called, deep-equal-asserted against the same document afterward).
- `npx vitest run` (Core suite): **330/330 passing**, unaffected.
- `npx vitest run --config typesetting-v2/tools/compare/vitest.config.ts` (Stage C suite): **21/21 passing**, unaffected.
- `npx tsc --noEmit`: 0 new errors (same pre-existing `src/app/layout.tsx` baseline).
- `git status`/`git diff --stat`: confirmed `src/`, Production, `package.json`, the lockfile, and the root `vitest.config.ts` are all byte-for-byte unmodified.

---

## 13. Human Visual QA Instructions

1. **Regenerate the artifact** (only needed if you want to re-run it yourself — a current copy already exists at the path below): `npx vitest run --config typesetting-v2/tools/preview-dev-adapter/vitest.config.ts`
2. **Open the file directly in a browser** (no server needed): `typesetting-v2/qa/visual/stage-d/index.html`
3. Use the top navigation links to jump between the 11 fixtures — start with **F20 — Canonical Regression Sentence** and **Long Non-Repeating Prose**.
4. Click the underlined **"toggle DEBUG VIEW"** text near the top of the page to reveal per-unit source spans, kinds, and line/column indices.
5. Check: page count, line-break correspondence, column flow, the manual-page-break marker's position, paragraph-break/indent/blank-line behavior, and that ruby/TCY/dash/ellipsis/image all show their **"prov"** badge (provisional, not final typography).
6. **Compare against the current live TateSpun app's own Preview** (a separate window/tab — this artifact does not embed or replace it) using the same manuscript text, per the first-visual-gate criteria already defined in `PREVIEW_DEVELOPMENT_ADAPTER_PLAN.md` §12: page count, line-break correspondence, column flow, manual-break position, Natural Pitch "feel," obvious ruby/TCY grouping, image space reservation. **Not yet judged at this gate:** final font aesthetics, ruby optical overhang, TCY shaping, dash/ellipsis micro-alignment, Publication PDF quality.

---

## 14. Known Non-Goals

Not attempted or closed by this Loop, per its own scope: P3-O03 (TCY visual shaping), P3-O04/O05 (dash/ellipsis optical alignment), P3-O06 (ruby overhang values), P3-O08 (Publication Renderer), P3-O09 (final Preview Renderer selection — this adapter does not decide it), F06 (hanging punctuation), the ruby-placement Core wiring gap (still OPEN — this Loop's ruby painting choice, §7, did not require or motivate closing it), and `long-prose`'s residual (§9). No Production feature flag, route, or UI-C work was created.

---

## 15. Stage D Human Gate Status

**IMPLEMENTED / awaiting Human Visual QA.** This document is not a substitute for that review — it records what was built and how to inspect it. No claim is made here about whether the painted result looks *correct* by Human judgment; that determination is explicitly reserved for the Human, per §13's instructions and the first-visual-gate criteria already on record.
